# My language
- react virtual dom is a js object, that is cheaper to modify compared to real dom, we only update the dom based on diff

# Textbook
React uses a Virtual DOM—an in-memory JS representation of the UI—to compute minimal DOM mutations via diffing, reducing expensive direct DOM operations.